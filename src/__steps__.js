/* 
1. craate a firbase project
2. Create a web application
3. npm i firebase
4. save firebase config and export default app
5. Build > Authentication >enable Sign in method
6. create sing up and login routes
*/

/* 
1. create a context provider
2. create a context provider set provider
3. set the children props
4. set context value
5. set the provider
*/

/*
 *Hosting
 * One time per pc
 * --------------
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 
 * For each project one time 
 * 1. firebase init
 * 2. proceed 
 * 3. hosting : firebase (up and down arrow) use space key for selection
 * 4. existing project
 * 5. select the project carefully
 * 6. whics projects as public directory: dist
 * 7. single page project : yes
 *  8. continuous diployment: no
 * 
 * For every time deploy
 * npm run build
 * firbase deploy
 * 
 * 
 */
